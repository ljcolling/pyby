

/*
pub fn between_d(m1: f64, m2: f64, s1: f64, s2: f64, n: f64) -> f64 {
    let md_diff = m1 - m2;
    let sd_pooled = sd_pooled(s1, s2, n);
    let d = md_diff / sd_pooled;
    return d;
}


pub fn sd_pooled(s1: f64, s2: f64, n: f64) -> f64 {
    ((((n - 1.0) * s1.powi(2)) + ((n - 1.0) * s2.powi(2))) / (n + n - 2.0)).sqrt()
} */
